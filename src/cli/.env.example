# ============================================
# 37AC CLI 客户端环境变量配置模板
# ============================================
# 复制此文件为 .env 并填入你的真实配置值
# cp .env.example .env

# --- 节点服务配置 ---
TCP_HOST=127.0.0.1
TCP_PORT=13137
LOCAL_PORT=
NODE_ID=1
TOKEN=your-node-token

# --- 心跳配置 ---
HEARTBEAT_INTERVAL_SEC=15
HEARTBEAT_RESPONSE_TIMEOUT_SEC=30
HEARTBEAT_MISS_LIMIT=3
RECONNECT_DELAY_SEC=10

# --- 任务配置 ---
MAX_TASKS=5

# --- 第三方大模型识别配置 ---
# 启用后节点会优先使用多模态 API 代替本地 ResNet 模型
LLM_RECOGNITION_ENABLED=False
LLM_API_KEY=your-deepseek-api-key
LLM_API_URL=https://api.deepseek.com/v1/chat/completions
LLM_MODEL_NAME=deepseek-v4.1-pro
LLM_TIMEOUT_SEC=30
LLM_PROMPT_TEMPLATE=

# --- YOLO 检测配置 ---
# YOLO（快速定位）+ ResNet（角色分类）
YOLO_ENABLED=True
# 模型文件路径（默认指向项目根目录的 yolov8n.pt）
YOLO_MODEL_PATH=
# 检测置信度阈值（低于此值的目标被忽略）
YOLO_CONFIDENCE=0.25

# --- 训练配置 ---
DATASET_DIR=W:/Img
NUM_EPOCHS=50
BATCH_SIZE=16
IMAGE_SIZE=224
LEARNING_RATE=1e-4
# 学习率调度: 预热5轮 ↗ → 余弦退火 ↘ FINE_TUNE_LR → 固定 FINE_TUNE_LR 微调
FINE_TUNE_EPOCH=40     # 第 N 轮后进入固定微调阶段
FINE_TUNE_LR=1e-5      # 微调阶段学习率
# 正则化与数据增强
WEIGHT_DECAY=1e-4      # 权重衰减（L2正则化）
LABEL_SMOOTHING=0.1    # 标签平滑（缓解过拟合）
GRAD_CLIP_NORM=1.0     # 梯度裁剪最大范数
# 早停（连续 X 轮无提升即停止，0=禁用）
EARLY_STOP_PATIENCE=10
# 验证集比例（0=不使用验证集）
VAL_SPLIT_RATIO=0.2
# DataLoader 工作进程数
NUM_WORKERS=2
MODEL_FILENAME=37ac-v0.0.1.pth
YOLO_ENABLED=True
YOLO_CONFIDENCE=0.25
MAX_IMAGES_PER_ROLE=100

# --- 设备配置 ---
# 自动选择设备（GPU/CPU）
AUTO_DEVICE=True
# 强制指定设备（cpu/cuda），AUTO_DEVICE=False 时生效
DEVICE=cpu
# 使用 DirectML (AMD GPU) — 适用于 RX 580 等 AMD 显卡
USE_DIRECTML=False

# --- 调试模式 ---
TSAC_DEBUG=False
