[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --cov=src --cov-report=term-missing
markers =
    slow: 需要较长时间执行的测试（如模型训练）
    network: 需要网络连接的测试
    db: 需要数据库连接的测试
    gpu: 需要 GPU 的测试